[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-rewards-backend"
version = "0.1.0"
description = "Hosted FastAPI backend for Agent Rewards (B0 #2/#3)"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["backend"]
